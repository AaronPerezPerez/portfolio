{
  "name": "aaron-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name portfolio",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name portfolio --branch preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.5",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
