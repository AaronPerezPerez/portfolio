{
  "name": "aaron-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:local": "astro build && wrangler pages dev dist",
    "dev:remote": "astro build && wrangler pages deploy dist --branch preview",
    "start:dev": "astro dev --host",
    "generate-cv": "node scripts/generate-cv-pdf.mjs",
    "build": "astro build && npm run generate-cv",
    "preview": "astro preview",
    "deploy": "astro build && npm run generate-cv && wrangler pages deploy dist",
    "deploy:preview": "astro build && wrangler pages deploy dist --branch preview",
    "astro": "astro",
    "db:migrate:local": "wrangler d1 execute portfolio-chat --local --file=./drizzle/0000_crazy_zarda.sql && wrangler d1 execute portfolio-chat --local --file=./drizzle/0001_mature_changeling.sql",
    "db:migrate:remote": "wrangler d1 execute portfolio-chat --remote --file=./drizzle/0000_crazy_zarda.sql && wrangler d1 execute portfolio-chat --remote --file=./drizzle/0001_mature_changeling.sql",
    "db:query": "wrangler d1 execute portfolio-chat --remote --command",
    "db:studio": "npx drizzle-kit studio",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "@astrojs/sitemap": "^3.6.0",
    "@simonwep/pickr": "^1.9.1",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.5",
    "chart.js": "^4.5.1",
    "chartjs-adapter-date-fns": "^3.0.0",
    "date-fns": "^4.1.0",
    "drizzle-orm": "^0.45.1",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251216.0",
    "@vitest/coverage-v8": "^4.0.16",
    "@vitest/ui": "^4.0.16",
    "drizzle-kit": "^0.31.8",
    "happy-dom": "^20.0.11",
    "puppeteer": "^24.34.0",
    "vitest": "^4.0.16",
    "wrangler": "^4.54.0"
  }
}
